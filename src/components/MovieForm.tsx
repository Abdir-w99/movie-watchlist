function MovieForm() {
  return (
    <div>
      <h2>Add Movie</h2>

      <form>
        <input type="text" placeholder="Movie title" />
        <button type="submit">Add</button>
      </form>
    </div>
  );
}

export default MovieForm;
